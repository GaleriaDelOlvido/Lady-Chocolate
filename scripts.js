// Get the menu section
const menuSection = document.getElementById("menu");

// Add a click event listener to the menu section
menuSection.addEventListener("click", function() {
  // Scroll to the top of the page
  window.scrollTo(0, 0);
});